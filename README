The purpose of this lab is to implement a hash table with closed hashing in c++. I was required to implement two hash tables, one with quadratic probing and one with double hashing using an array of structures (not arrays in STL).
